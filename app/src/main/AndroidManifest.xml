<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragmentos">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!--PARA VER EL ESTADO DE LA RED-->
    <uses-permission android:name="android.permission.INTERNET"/> <!--PARA CONECTARSE A INTERNET-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fragmentos">
        <activity android:name=".SplashScreen" android:theme="@style/SplashScreenTheme">
            <intent-filter> <!--esto define la actividad que se muestra primero-->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:windowSoftInputMode="adjustPan">
            <!--usualmente va aca-->
        </activity>

    </application>

</manifest>